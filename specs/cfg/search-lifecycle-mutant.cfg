SPECIFICATION Spec
CONSTANTS
    GENS = 2
    PATHS = {p1, p2}
    REV_MAX = 2
    WARM_MAX = 4
\* Once every generation, revision and warm budget is spent, no action
\* is enabled — a model artifact, not a protocol deadlock.
CHECK_DEADLOCK FALSE
INVARIANTS
    TypeOK
    RowsCurrent
    RowsInScope
    StaleAcceptsNever
    CompletionHonest
    WarmBounded
