\* One client and two worker incarnations: joint stream/Store product,
\* with a two-chunk consumer window and both same/changed namespace
\* recovery paths. Two-client campaigns below exhaust each boundary.
SPECIFICATION Spec
CONSTANTS
 Clients = {c1}
 Workers = {w1, w2}
 Streams = {s1}
 MAXGEN = 1
 WINDOW = 2
 QCAP = 1
 MODE = 0
 MUTATION = 0
CHECK_DEADLOCK FALSE
INVARIANTS
 TypeOK
 BoundedOutstanding
 NoStaleEffect
 NoStaleCommit
 ForeignStopRefused
 ExitAfterOutput
 DirtyUntilProof
 RecoveryInNamespace
