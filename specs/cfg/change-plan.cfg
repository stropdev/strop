SPECIFICATION Spec
CONSTANTS
DOCS = {1, 2, 3}
MAXREV = 2
MUTATION = 0
ADVERSITY = TRUE
\* The revision cap can starve a pending apply (a document bumped to
\* MAXREV before its turn can neither commit nor mismatch-refuse) — a
\* finite model artifact, not a protocol deadlock, so deadlock checking
\* is off. Terminal still gives done/cancelled runs a proper stutter.
\* The VF04 member fixture (projection rows, protected chrome row, the
\* overlapping excerpt pair) is defined in the module: this TLC version's
\* cfg syntax accepts only flat values.
CHECK_DEADLOCK FALSE
INVARIANTS
TypeOK
NoStaleApplication
HonestReceipt
PartialProgressHonest
ProtectedNeverApplied
NoDuplicateMutation
MemberSourceBound
GroupUniformOutcome
