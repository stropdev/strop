\* Notify faulty variant 5: a full native queue drops the event without
\* recording overflow — the only staleness sign vanishes silently. Must
\* die by EXACTLY CoalesceNeverLosesStaleness.
SPECIFICATION Spec
CONSTANTS
    CLIENTS = {c1, c2}
    INC_MAX = 2
    GEN_MAX = 2
    REV_MAX = 3
    QMAX = 1
    MUTATION = 5
CHECK_DEADLOCK FALSE
INVARIANTS
    TypeOK
