\* Notify faulty variant 9: an unsupported namespace is served by an
\* invisible full-tree crawl posing as native coverage. Must die by
\* EXACTLY CoverageHonest.
SPECIFICATION Spec
CONSTANTS
    CLIENTS = {c1, c2}
    INC_MAX = 2
    GEN_MAX = 2
    REV_MAX = 3
    QMAX = 1
    MUTATION = 9
CHECK_DEADLOCK FALSE
INVARIANTS
    TypeOK
