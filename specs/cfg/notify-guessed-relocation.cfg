\* Notify faulty variant 3: an ambiguous rename (cookie pairing failed)
\* relocates the document binding straight off the guessed pair. Must die
\* by EXACTLY AmbiguousNeverRelocates.
SPECIFICATION Spec
CONSTANTS
    CLIENTS = {c1, c2}
    INC_MAX = 2
    GEN_MAX = 2
    REV_MAX = 3
    QMAX = 1
    MUTATION = 3
CHECK_DEADLOCK FALSE
INVARIANTS
    TypeOK
