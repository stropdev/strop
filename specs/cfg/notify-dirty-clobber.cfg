\* Notify faulty variant 7: a reload applies its snapshot over a dirty
\* buffer, clobbering unsaved edits. Must die by EXACTLY
\* DirtyNeverClobbered.
SPECIFICATION Spec
CONSTANTS
    CLIENTS = {c1, c2}
    INC_MAX = 2
    GEN_MAX = 2
    REV_MAX = 3
    QMAX = 1
    MUTATION = 7
CHECK_DEADLOCK FALSE
INVARIANTS
    TypeOK
