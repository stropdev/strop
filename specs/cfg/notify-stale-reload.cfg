\* Notify faulty variant 8: a stale in-flight snapshot publishes over a
\* newer baseline, clearing newer state. Must die by EXACTLY
\* StaleReloadNeverClears.
SPECIFICATION Spec
CONSTANTS
    CLIENTS = {c1, c2}
    INC_MAX = 2
    GEN_MAX = 2
    REV_MAX = 3
    QMAX = 1
    MUTATION = 8
CHECK_DEADLOCK FALSE
INVARIANTS
    TypeOK
