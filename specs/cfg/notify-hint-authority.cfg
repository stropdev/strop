\* Notify faulty variant 1: a delivered hint is applied as authoritative
\* content without observation. Must die by EXACTLY HintsNeverAuthority.
SPECIFICATION Spec
CONSTANTS
    CLIENTS = {c1, c2}
    INC_MAX = 2
    GEN_MAX = 2
    REV_MAX = 3
    QMAX = 1
    MUTATION = 1
CHECK_DEADLOCK FALSE
INVARIANTS
    TypeOK
