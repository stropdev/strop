\* Notify faulty variant 4: freshness is claimed while coverage is
\* lost/degraded, a rescan is owed, or invalidations are pending. Must
\* die by EXACTLY FreshRequiresCoverage.
SPECIFICATION Spec
CONSTANTS
    CLIENTS = {c1, c2}
    INC_MAX = 2
    GEN_MAX = 2
    REV_MAX = 3
    QMAX = 1
    MUTATION = 4
CHECK_DEADLOCK FALSE
INVARIANTS
    TypeOK
