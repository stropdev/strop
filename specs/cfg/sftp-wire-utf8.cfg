\* SftpWire coverage for the Validate transition: with a file whose bytes
\* do not decode, the session must end in the typed INVALID_UTF8 fault,
\* never in SUCCESS or a partial publish. This run EXPECTS
\* WitnessNoUtf8Failure to be violated.
SPECIFICATION Spec
CONSTANTS
    MAXPACKET = 3
    MAXSNAP = 4
    CHUNK = 2
    FSIZE = 3
    FILE_UTF8 = FALSE
    MAXSEQ = 8
    MUTATION = 0
CHECK_DEADLOCK FALSE
INVARIANTS
    WitnessNoUtf8Failure
