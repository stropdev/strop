\* Distinct artifact digests in one selected context; a cached object
\* cannot stand in for a different release build's verified bytes.
SPECIFICATION Spec
CONSTANTS
 Clients = {a}
 Contexts = {ctx1}
 Digests = {sha1, sha2}
 MUTATION = 0
CHECK_DEADLOCK FALSE
INVARIANTS
 TypeOK
 VerifiedActivation
 ChosenContext
 OwnedCleanup
 NoRetireLive
 ConsentBeforeUpload
