\* LspWire faulty variant 6: a reply settles an already-terminal request
\* a second time — the exactly-one-terminal-event contract (R9) breaks.
\* Must die by EXACTLY TerminalOnce.
SPECIFICATION Spec
CONSTANTS
    DOCS = {d1, d2}
    VER_MAX = 5
    REQ_MAX = 2
    QMAX = 3
    PEND_MAX = 2
    MUTATION = 6
CHECK_DEADLOCK FALSE
INVARIANTS
    TypeOK
    WireOrdered
    VersionsIncrease
    CoalesceLegal
    BindingOnlyIfAdmitted
    TerminalOnce
    RequestAccounted
    LateNeverApplies
    Bounded
