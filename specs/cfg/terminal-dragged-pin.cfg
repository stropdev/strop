\* Terminal faulty variant 2: frames auto-install while a pane inspects —
\* live output drags the pinned snapshot under the user's selection.
\* Must die by EXACTLY PinnedNeverDragged.
SPECIFICATION Spec
CONSTANTS
    SESSIONS = {s1, s2}
    REV_MAX = 2
    MUTATION = 2
CHECK_DEADLOCK FALSE
INVARIANTS
    TypeOK
    FrameFresh
    PinnedNeverDragged
    TerminalAbsorbing
    ExitBoundary
    ExitAnnouncedOnce
    InputNeverToDead
    GeometryOwned
    OutputNeverMutates
