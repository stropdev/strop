\* Two independent clients and Docker/SSH contexts. Their cache
\* artifact digest is equal, so a race on the exact object is live.
SPECIFICATION Spec
CONSTANTS
 Clients = {a, b}
 Contexts = {ctx1, ctx2}
 Digests = {sha}
 MUTATION = 0
CHECK_DEADLOCK FALSE
INVARIANTS
 TypeOK
 VerifiedActivation
 ChosenContext
 OwnedCleanup
 NoRetireLive
 ConsentBeforeUpload
