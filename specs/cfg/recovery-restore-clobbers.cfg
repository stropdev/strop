\* Recovery faulty variant 4: restore writes the checkpoint bytes back to
\* the source path — clobbering changed disk content and recreating
\* deleted targets.
\* Must die by EXACTLY RestoreNeverWritesDisk.
SPECIFICATION Spec
CONSTANTS
    DOCS = {d1, d2}
    REMOTE = {d2}
    REV_MAX = 2
    COHORT_MAX = 3
    MUTATION = 4
CHECK_DEADLOCK FALSE
\* Budgets are model artifacts, not protocol states: a spent cohort
\* budget must not freeze staleness the u64 counter would recapture.
CONSTRAINT
    CohortBudgetHonest
INVARIANTS
    TypeOK
    CohortCoherent
    DurableMatchesLastComplete
    SaveRetiresOnlySuperseded
    CheckpointNeverCleans
    RemoteConsentGated
    RestoreNeverWritesDisk

