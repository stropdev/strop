SPECIFICATION Spec
CONSTANTS
    DOCS = {1, 2}
    PANES = {1, 2}
    REQS = {1, 2}
    MAXREV = 3
    MAXGEN = 2
\* Same rationale as editor-protocol.cfg: no liveness claim, no deadlock
\* check. The mutant must die by an invariant violation, not by deadlock.
CHECK_DEADLOCK FALSE
INVARIANTS
    TypeOK
    NoStalePane
    NoWrongDocument
    RevisionTracksPublications
    TicketOneShot
    NoMisapply
