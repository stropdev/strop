\* Two clients, two workers and one native stream: foreign stop,
\* delayed chunks, worker restart and bounded credits interleave.
SPECIFICATION Spec
CONSTANTS
 Clients = {c1, c2}
 Workers = {w1, w2}
 Streams = {s1}
 MAXGEN = 1
 WINDOW = 2
 QCAP = 1
 MODE = 1
 MUTATION = 0
CHECK_DEADLOCK FALSE
INVARIANTS
 TypeOK
 BoundedOutstanding
 NoStaleEffect
 NoStaleCommit
 ForeignStopRefused
 ExitAfterOutput
 DirtyUntilProof
 RecoveryInNamespace
