\* Notify faulty variant 2: an event stamped by a superseded identity
\* (old generation/incarnation, reused descriptor) is acted on instead of
\* dropped. Must die by EXACTLY StaleIdentityNeverActs.
SPECIFICATION Spec
CONSTANTS
    CLIENTS = {c1, c2}
    INC_MAX = 2
    GEN_MAX = 2
    REV_MAX = 3
    QMAX = 1
    MUTATION = 2
CHECK_DEADLOCK FALSE
INVARIANTS
    TypeOK
