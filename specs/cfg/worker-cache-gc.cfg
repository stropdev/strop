\* Two clients may execute distinct builds in either selected context.
\* Both must share the same owner-private lock and lease snapshot.
SPECIFICATION Spec
CONSTANTS
 Clients = {a, b}
 Contexts = {ctx1, ctx2}
 Digests = {sha, other}
 MUTATION = 0
CHECK_DEADLOCK FALSE
INVARIANTS
 SnapshotComplete
 TypeOK
 LiveObject
 ScopedRetirement
 NoRetireLive
 StalePinned
