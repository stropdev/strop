\* Two clients and two workers: Store admission, receipt loss,
\* reconnect and namespace recovery interleave for distinct owners.
SPECIFICATION Spec
CONSTANTS
 Clients = {c1, c2}
 Workers = {w1, w2}
 Streams = {s1}
 MAXGEN = 1
 WINDOW = 2
 QCAP = 1
 MODE = 2
 MUTATION = 0
CHECK_DEADLOCK FALSE
INVARIANTS
 TypeOK
 BoundedOutstanding
 NoStaleEffect
 NoStaleCommit
 ForeignStopRefused
 ExitAfterOutput
 DirtyUntilProof
 RecoveryInNamespace
