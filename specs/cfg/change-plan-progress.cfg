SPECIFICATION Spec
CONSTANTS
DOCS = {1, 2}
MAXREV = 2
MUTATION = 0
\* Progress is claimed only against a quiescent adversary, under weak
\* fairness of the plan steps (Spec's WF assumption): with no outside
\* writer the gateway check never blocks a commit, so the plan always
\* reaches a receipt or a cancellation. With adversity the claim still
\* holds in every behavior the revision cap does not starve; the cap is
\* a model artifact, so deadlock checking stays off here too.
ADVERSITY = FALSE
CHECK_DEADLOCK FALSE
INVARIANTS
TypeOK
NoStaleApplication
HonestReceipt
PartialProgressHonest
PROPERTY PlanSettles
