\* Install faulty variant 1: the rename lands BEFORE digest verification.
\* Must die by EXACTLY VerifiedBeforeActivation.
SPECIFICATION Spec
CONSTANTS
    MUTATION = 1
CHECK_DEADLOCK FALSE
INVARIANTS
    TypeOK
    InstalledComplete
    VerifiedBeforeActivation
    ChannelGate
    NoClobberOnFailure
    ReceiptNeverAhead
    StaleNeverPublishes
