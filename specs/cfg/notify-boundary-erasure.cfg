\* Notify faulty variant 6: scan completion erases ALL pending
\* invalidations, including ones newer than the boundary sequence. Must
\* die by EXACTLY ScanNeverErasesNewer.
SPECIFICATION Spec
CONSTANTS
    CLIENTS = {c1, c2}
    INC_MAX = 2
    GEN_MAX = 2
    REV_MAX = 3
    QMAX = 1
    MUTATION = 6
CHECK_DEADLOCK FALSE
INVARIANTS
    TypeOK
