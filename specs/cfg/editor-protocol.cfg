SPECIFICATION Spec
CONSTANTS
    DOCS = {1, 2}
    PANES = {1, 2}
    REQS = {1, 2}
    MAXREV = 3
    MAXGEN = 2
\* Liveness/progress is not modeled (see the spec header): once every
\* incarnation budget is spent, no action is enabled — that is a model
\* artifact, not a protocol deadlock, so deadlock checking is off.
CHECK_DEADLOCK FALSE
INVARIANTS
    TypeOK
    NoStalePane
    NoWrongDocument
    RevisionTracksPublications
    TicketOneShot
    NoMisapply
