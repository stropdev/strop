\* Notify coverage template: the honest model (MUTATION = 0), same small
\* budgets as notify.cfg. Used only as the base for per-witness configs —
\* each WitnessNo* property must be REACHABLE (the check expects the
\* witness invariant to fail).
SPECIFICATION Spec
CONSTANTS
    CLIENTS = {c1, c2}
    INC_MAX = 2
    GEN_MAX = 2
    REV_MAX = 3
    QMAX = 1
    MUTATION = 0
CHECK_DEADLOCK FALSE
INVARIANTS
    TypeOK
