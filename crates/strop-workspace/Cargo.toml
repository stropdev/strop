[package]
name = "strop-workspace"
description = "Shared resource identity: remote address forms, filesystem namespaces, resource locations"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
strop-core = { path = "../strop-core", version = "0.27.0" }
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"
