[package]
name = "strop-grammar"
description = "strop grammar: pure vim operator-pending resolver (motions, text objects, counts)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
strop-core = { path = "../strop-core", version = "0.2.0" }
ropey = "1"
