[package]
name = "strop-worker-client"
description = "Editor-side client of the 0058 unified worker: leased spawn of the matching executable, handshake readiness, typed failures, retirement — never a silent fallback"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
strop-core = { path = "../strop-core", version = "0.35.0" }
strop-worker-protocol = { path = "../strop-worker-protocol", version = "0.35.0" }
strop-workspace = { path = "../strop-workspace", version = "0.35.0" }
parking_lot = "0.12"
sha2 = "0.10"
thiserror = "2"

[dev-dependencies]
strop-worker = { path = "../strop-worker", version = "0.35.0" }
tempfile = "3"
